# Glim

桌面快速翻译工具。按一个键，把剪贴板里的东西翻译或查词，看完即走。
Tauri v2 + React + TypeScript，前端只渲染，热键/剪贴板/窗口/网络全在 Rust 侧。

## 先读这三个文件

1. **`docs/PLAN.md`** —— 方案原文。这是唯一的事实来源，**不要凭印象改它**。
   §8 是门禁清单，§9 是审核流程，§10 是给执行者的约束条款，附录 B 是变更记录。
2. **`gates/M0.md`** —— M0 自检报告 + 复核记录。已冻结。
3. **`gates/M1.md`** —— M1 自检报告。**6 条通过，1.2 未通过，等待裁定。**

## 当前状态

| 里程碑 | 状态 |
|---|---|
| M0 骨架与窗口 | ✅ 冻结 @2026-09-03 |
| M1 打通翻译 | ⏸ 6/7 通过，1.2 未通过，**等审核者裁定** |
| M2 及以后 | 未开始，裁定前不得开始（§9.2） |

分支：`m0-skeleton`。

### 等审核者定的四件事（见 gates/M1.md 结论）

1. 门禁 1.2 首字符延迟未通过（中位 1816ms / 阈值 1200ms）如何处置
2. §4.2 的 15s 超时与实测 17.4s 成功响应的冲突
3. 6 项实现偏差是否认可
4. 1.4 引导卡片是否需要补证据（`gates/m1-verify.ps1 -Pause`）

### 已裁定、记在附录 B 的两条

- `OpenAICompatProvider`（接国内直连服务）排在 **M1 冻结之后**单独一轮，不并入 M1。
- **不做**「按网络环境自动切换 Provider」。理由：查 IP 要发网络请求违反 §6；
  真实变量是代理此刻开没开而非所在国家；猜错的代价不对称。

## 铁律（违反即判门禁未通过）

来自 §10，逐条都吃过亏：

1. **不得引入 §1 依赖清单之外的第三方库**。需要时停下来问。
   已因此绕开 `uuid`（改用计数器）、`async-trait`（trait 拆成同步两段）、
   `tokio`（超时用独立 OS 线程看门狗）、`futures-util`（用 `Response::chunk()`）。
2. **M4 之前不做 UI 样式**。现在全是裸标签，没有一行 CSS。
3. **不得「统一」查词非流式 / 翻译流式的差异**（§3.3），那是刻意设计。
4. **不得实现 §11 推迟清单里的东西**，包括「顺手加一下」。
5. **不得修改 §4.1 的 IPC 命令与事件名**。语义扩展要写进报告等审核。
6. **遇到方案未规定的设计决策，停下来问**，不要自行发挥。
7. **不得为了通过门禁而放宽门禁**。达不到就附实测数据如实报告。

## 开发环境的硬约束

- **必须在 Windows 上验证。** 门禁全是 Win32、PowerShell、真实网络。
- 交叉检查（Linux 上也能跑，只检查不链接）：
  ```
  cd src-tauri && cargo check --target x86_64-pc-windows-msvc
  ```
- 构建：`pnpm tauri build --no-bundle`
- **构建前必须先杀掉 Glim 进程**，否则 exe 被占用，报 `Access is denied (os error 5)`。
- 跑门禁脚本时**不要碰鼠标键盘**，它在合成输入；碰了会污染结果
  （M0 的 0.1c 就因为一次误触的左键点击连续两轮判失败）。

## 环境相关（本机特有，不是代码问题）

- Google API 在国内需代理。`reqwest` 读 `HTTPS_PROXY` 环境变量，
  **必须从设了该变量的同一个终端启动 Glim**，否则读不到。
- 配置：`%APPDATA%\glim\settings.json`（模型串、端点、热键、语言对）
- API Key：Windows 凭据管理器，service 名 `glim/provider/<provider_id>`，
  **不在任何明文文件里**。
- 日志：`%APPDATA%\glim\hotkey-timing.log`。门禁靠它断言 —— 面板内容在
  WebView 里，PowerShell 读不到 DOM，所以断言的是 Rust 侧写下的判定结果。

## 工具脚本

```
pwsh -File tools/models.ps1                    # 列出当前 Key 可用的模型
pwsh -File tools/models.ps1 -Set <模型串>       # 写进配置，不用改代码、不用重新构建
pwsh -File tools/models.ps1 -Bench             # 首字节延迟基准，绕开本程序代码
pwsh -File gates/m0-verify.ps1                 # M0 门禁
pwsh -File gates/m1-verify.ps1 [-Pause]        # M1 门禁
```

## 已经踩过的坑，别再踩

### PowerShell（这些全都真实发生过）

- `ConvertTo-Json` 会把**单元素数组**序列化成对象。构造 API 请求体要手写 JSON 字面量。
- 函数会把**所有未捕获的输出**并入返回值。函数里的提示语必须用 `Write-Host`，
  否则调用方拿到的是「提示语 + 真值」的数组。
- `Where-Object` 只命中一条时返回的是**字符串本身**，此时 `[-1]` 取到的是
  **最后一个字符**，不是最后一条。要用 `@()` 包住。
- `Substring(0, n)` 在字符串短于 n 时抛异常。
- 日志是追加的，轮询日志必须**记录基线行数**，否则会匹配到上一轮的残留。

### 产品代码

- **窗口显示/隐藏必须走 Win32，不能用 tao 的 `show()`** —— tao 会拿自己缓存的
  `WindowFlags` 重写整个 ex-style，把 `WS_EX_TOOLWINDOW` / `WS_EX_NOACTIVATE`
  抹掉、把 `WS_EX_APPWINDOW` 加回来，门禁 0.2 直接失效。
- **Esc 的注册/注销必须在工作线程上做**。在热键回调链里直接注册会重入
  非重入锁，主线程当场死锁。
- **SSE 必须按字节缓冲再切行**。按 String 缓冲会把汉字拆在 chunk 边界上。
- **面板带 `WS_EX_NOACTIVATE`，拿不到键盘焦点。** 所以 API Key 走「从剪贴板
  粘贴」按钮而不是输入框 —— 做输入框就得摘掉该样式抢焦点，会破坏已冻结的门禁 0.3。
- **点击外部轮询在隐藏期间不能清零 `was_down`**，否则面板一可见就把
  「一直按着的左键」误判成新的按下沿，面板一闪即消失。

### 方法

- **写完自己没法验证的东西之前，先想清楚验证手段。**
  Rust 有 `cargo check` 交叉验证，所以基本一次过；PowerShell 没有，
  于是连续六个 bug 都靠人工跑才暴露，每个都消耗一轮往返。
- **不确定的事不要猜着改代码。** 先加打点量出来，再动手。
  默认模型串是猜的（404）、错误信息想当然（吞掉了原因）、bench 的 JSON
  想当然（400）—— 凡是没实测过的假设基本都是错的。
- **报告里不许出现「至少有一次成功即通过」这种判定。**
  曾把 1/10 成功率判成通过，正是 §10 第 7 条禁止的自欺。
